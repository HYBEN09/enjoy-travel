import styled from 'styled-components';

export const ButtonWrapper = styled.button`
  padding: 10px;
  gap: 12px;
  width: 327px;
  height: 64px;
  background: var(--primary);
  border-radius: 100px;
`;
